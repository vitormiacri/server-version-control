import { makeStyles } from '@material-ui/core';

export const InputClass = makeStyles(theme => ({
  root: {
    marginBottom: '2rem',
  },
}));

export const SwitchClass = makeStyles(theme => ({
  root: {
    marginBottom: '1rem',
  },
}));
